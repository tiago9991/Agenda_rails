<%= form_with(model: phone, html: { class: "basic-grey"}) do |form| %>
  <% if phone.errors.any? %>
    <div class = "basic-grey" style="color: red">
      <h2><%= pluralize(phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
        <% phone.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <h1>New Phones</h1>

  <div>
    <%= form.label :phones, style: "display: block" %>
    <%= form.text_field :phones %>
  </div>

  <div>
    <%= form.label :contact_id, style: "display: block" %>
    <%= form.text_field :contact_id %>
  </div>

  <div>

    <%= form.submit%>
  </div>
<% end %>
